{
  "name": "dori-worker",
  "version": "1.0.0",
  "description": "DORI Dashboard — Cloudflare Worker offline page",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
